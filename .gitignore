# Windows
Thumbs.db
Desktop.ini
ehthumbs.db

# Compiled output / build artifacts
*.user
*.suo

# This repo intentionally ships the compiled exe in Releases,
# but the working exe is tracked too since it's a single-file build.